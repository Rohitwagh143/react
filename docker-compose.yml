version: '3'
services:
  react-app:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "80:80"
  tests:
    build:
      context: .
      dockerfile: Dockerfile.dev

    volumes:
      - app/node_modules
    
    command: ["npm","run","test"]